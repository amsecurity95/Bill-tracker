// Bills routes
// Note: In NestJS, routes are defined in controllers using decorators
// This file is provided for reference/documentation purposes

/**
 * Bills API Routes:
 * 
 * POST   /bills          - Create a new bill
 * GET    /bills          - Get all bills
 * GET    /bills/:id      - Get a specific bill by ID
 * PATCH  /bills/:id      - Update a bill
 * DELETE /bills/:id      - Delete a bill
 * 
 * These routes are handled by:
 * - BillsController in src/bills/bills.controller.ts
 * - BillsService in src/bills/bills.service.ts
 */

module.exports = {
  // Routes are defined in NestJS controllers, not here
  // See: src/bills/bills.controller.ts
};

